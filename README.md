# Simulation

## Nix Setup

1. Change directory to the root of this project
2. Install dependencies: `nix-shell -p python312Packages.pygame`
3. Run the skeleton simulation: `python3 sims/skeleton.py`
